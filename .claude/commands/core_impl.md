# Continue Passpal $ARGUMENTS core implementation

Continue implementing `core/$ARGUMENTS`.

## Tasks

1. Read `lib/core/$ARGUMENTS/implementation_progress.md`
2. Identify the next ISSUE to implement
3. Start implementing that ISSUE
4. Update the progress file accordingly

## Prompt

Read `lib/core/$ARGUMENTS/implementation_progress.md` and continue the next implementation task for `$ARGUMENTS`.

Follow these steps:

1. Check current progress
2. Find the next ISSUE to implement (must be unstarted and have all dependencies resolved)
3. Start implementing that ISSUE
4. After completing it, update `implementation_progress.md`
