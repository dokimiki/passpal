Inside @lib/core/$ARGUMENTS/document.md is the full design for the `$ARGUMENTS` core module.
As a skilled engineer, you are responsible for its implementation.

Please follow these instructions:

* Understand the design thoroughly think hard, including its interaction with other core modules.
* Break down the implementation into well-scoped issues, each representing a unit suitable for a Pull Request (PR).
* Ensure the scope of each issue/PR is small enough to be human-reviewable, including by me.
* Create a file named `IMPLEMENTATION_PROGRESS.md` under `lib/core/$ARGUMENTS`. Use it to:

  * List all implementation issues
  * Track the current progress
* Keep the file up-to-date so progress can resume from any interruption.
* Always keep this instruction in mind.
* Refer to `.dart` files and `.md` instructions of other core modules as needed.
* Do not implement yet.
